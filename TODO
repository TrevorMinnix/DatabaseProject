-Table for GC
-Actual functionality for GC (changing scores, display the icomplete nominee submissions)
-Enforcing deadlines (many ways)
-make login only valid for current session
-The nominator needs to confirm the form sent by the nominee before it is official
--The nominator is sent an email of a page to confirm the info.
-Session closing
-UI is pretty bad
-Lots and lots of testing
--need to check if the session relationships actually doing anything
